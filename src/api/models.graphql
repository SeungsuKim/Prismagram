type UserProfile {
  user: User!
  posts: [Post!]!
}

type FullPost {
  post: Post!
  comments: [Comment!]!
  likeCount: Int!
}

extend type User {
  fullName: String
  amIFollowing: Boolean!
}

extend type Post {
  isLiked: Boolean!
}